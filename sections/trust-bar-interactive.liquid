<style>
  :root { --k-cyan: #00D1FF; --k-dark: #050505; }
  .trust-bar-section { background-color: #000; padding: 40px 0; border-top: 1px solid #1a1a1a; font-family: 'Inter', sans-serif; }
  .trust-bar-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 15px; max-width: 1400px; margin: 0 auto; padding: 0 20px; }
  .trust-item-wrapper { background: var(--k-dark); border-radius: 12px; border: 1px solid #1a1a1a; transition: 0.3s; overflow: hidden; position: relative; }
  .trust-item-wrapper:hover { border-color: var(--k-cyan); }
  .trust-item-wrapper.is-active { border-color: var(--k-cyan); box-shadow: 0 0 15px rgba(0, 209, 255, 0.1); }
  .trust-header { display: flex; align-items: center; gap: 10px; padding: 15px; cursor: pointer; }
  .trust-icon-wrapper { width: 24px; color: var(--k-cyan); display: flex; align-items: center; }
  .trust-text { display: flex; flex-direction: column; }
  .trust-title { color: #fff; font-weight: 700; font-size: 11px; text-transform: uppercase; }
  .trust-subtitle { color: #666; font-size: 10px; }
  .trust-content { max-height: 0; overflow: hidden; transition: 0.3s ease-out; background: #080808; padding: 0 15px; }
  .trust-item-wrapper.is-active .trust-content { max-height: 500px; padding: 10px 15px 20px 15px; border-top: 1px solid #1a1a1a; }
  
  /* ESTILO ADD BLOCKS */
  .add-block-custom { background: #111; border-left: 3px solid var(--k-cyan); padding: 12px; border-radius: 4px; margin-bottom: 12px; }
  .add-block-custom p { color: #ccc; font-size: 11px; margin: 0; line-height: 1.5; }
  .trust-btn { background: var(--k-cyan); color: #000 !important; border: none; width: 100%; padding: 10px; border-radius: 5px; font-weight: 800; font-size: 10px; cursor: pointer; text-transform: uppercase; text-decoration: none !important; display: block; text-align: center; margin-bottom: 8px; }
  .trust-btn:hover { background: #fff; }

  @media (max-width: 1000px) { .trust-bar-grid { grid-template-columns: 1fr 1fr; } }
  @media (max-width: 600px) { .trust-bar-grid { grid-template-columns: 1fr; } }
</style>

<section class="trust-bar-section">
  <div class="trust-bar-grid">
    
    <div class="trust-item-wrapper">
      <div class="trust-header" onclick="toggleTrust(this)">
        <div class="trust-icon-wrapper"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 11l4 4L19 7"></path></svg></div>
        <div class="trust-text"><span class="trust-title">Envío Gratis</span><span class="trust-subtitle">Formulario</span></div>
      </div>
      <div class="trust-content">
        <form action="/contact" method="post" class="trust-form"><input type="text" placeholder="Nombre" class="trust-input" style="width:100%; margin-bottom:5px; background:#111; border:1px solid #222; color:#fff; padding:8px;"><button type="submit" class="trust-btn">Enviar</button></form>
      </div>
    </div>

    <div class="trust-item-wrapper">
      <div class="trust-header" onclick="toggleTrust(this)">
        <div class="trust-icon-wrapper"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="11" width="18" height="11" rx="2" ry="2"></rect><path d="M7 11V7a5 5 0 0110 0v4"></path></svg></div>
        <div class="trust-text"><span class="trust-title">Pago Seguro</span><span class="trust-subtitle">Opciones</span></div>
      </div>
      <div class="trust-content">
        {% if section.settings.add_block_text != blank %}
          <div class="add-block-custom"><p>{{ section.settings.add_block_text }}</p></div>
        {% endif %}
        
        {% if section.settings.link_1 != blank %}
          <a href="{{ section.settings.link_1 }}" class="trust-btn">{{ section.settings.label_1 }}</a>
        {% endif %}
        
        {% if section.settings.link_2 != blank %}
          <a href="{{ section.settings.link_2 }}" class="trust-btn" style="background:#333; color:#fff !important;">{{ section.settings.label_2 }}</a>
        {% endif %}
      </div>
    </div>

    <div class="trust-item-wrapper">
       <div class="trust-header" onclick="toggleTrust(this)"><div class="trust-icon-wrapper"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="5.5" cy="18.5" r="2.5"></circle><circle cx="18.5" cy="18.5" r="2.5"></circle><path d="M1 3h15v13H1zM16 8h4l3 3v5h-7V8z"></path></svg></div><div class="trust-text"><span class="trust-title">Entrega</span><span class="trust-subtitle">Rastreo</span></div></div>
       <div class="trust-content"><input type="text" placeholder="Nro Guía" style="width:100%; margin-bottom:5px; background:#111; border:1px solid #222; color:#fff; padding:8px;"><button class="trust-btn">Rastrear</button></div>
    </div>

    <div class="trust-item-wrapper">
       <div class="trust-header" onclick="toggleTrust(this)"><div class="trust-icon-wrapper"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 11.5a8.38 8.38 0 01-.9 3.8 8.5 8.5 0 01-7.6 4.7 8.38 8.38 0 01-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 01-.9-3.8 8.5 8.5 0 014.7-7.6 8.38 8.38 0 013.8-.9h.5a8.48 8.48 0 018 8v.5z"></path></svg></div><div class="trust-text"><span class="trust-title">Soporte</span><span class="trust-subtitle">24/7</span></div></div>
       <div class="trust-content"><div class="add-block-custom"><p>Escríbenos para ayudarte.</p></div></div>
    </div>

  </div>
</section>

<script>
function toggleTrust(element) {
  const wrapper = element.closest('.trust-item-wrapper');
  const isActive = wrapper.classList.contains('is-active');
  document.querySelectorAll('.trust-item-wrapper').forEach(item => item.classList.remove('is-active'));
  if (!isActive) wrapper.classList.add('is-active');
}
</script>

{% schema %}
{
  "name": "Trust Bar Acordeón",
  "settings": [
    {
      "type": "header",
      "content": "Configuración de Pagos (Bloque 2)"
    },
    {
      "type": "textarea",
      "id": "add_block_text",
      "label": "Texto del Add Block (Caja con borde)",
      "default": "Aceptamos todas las tarjetas y pagos seguros."
    },
    {
      "type": "text",
      "id": "label_1",
      "label": "Texto Botón 1",
      "default": "Pagar con PayPal"
    },
    {
      "type": "url",
      "id": "link_1",
      "label": "Enlace Botón 1"
    },
    {
      "type": "text",
      "id": "label_2",
      "label": "Texto Botón 2",
      "default": "Ver Métodos"
    },
    {
      "type": "url",
      "id": "link_2",
      "label": "Enlace Botón 2"
    }
  ],
  "presets": [{ "name": "Trust Bar Acordeón" }]
}
{% endschema %}