<style>
  /* Configuración General */
  .hero-ad-block {
    background-color: #000000;
    color: #ffffff;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 60px 10%;
    font-family: "Futura", "Trebuchet MS", sans-serif;
    gap: 40px;
    overflow: hidden;
  }

  /* Contenedor de Texto */
  .hero-text-content {
    flex: 1;
    z-index: 2;
  }

  .hero-text-content h1 {
    font-size: clamp(2rem, 5vw, 3.5rem);
    color: #ffffff;
    text-transform: uppercase;
    margin: 0;
    line-height: 1;
    /* EFECTO GLOW / RESPLANDOR */
    text-shadow: 0 0 15px rgba(255, 255, 255, 0.7), 0 0 30px rgba(255, 255, 255, 0.3);
  }

  .hero-text-content p {
    font-size: 1.2rem;
    color: #cccccc;
    margin-top: 20px;
    text-transform: none;
    letter-spacing: 0.5px;
  }

  /* Contenedor de Imagen (El "Add Block") */
  .hero-image-block {
    flex: 1;
    display: flex;
    justify-content: center;
    position: relative;
  }

  .hero-image-block img {
    width: 100%;
    max-width: 500px;
    height: auto;
    border-radius: 4px;
    /* Efecto de borde iluminado para la imagen */
    box-shadow: 0 0 20px rgba(255,255,255,0.1);
    transition: transform 0.5s ease;
  }
  
  .hero-image-block img:hover {
    transform: scale(1.02);
  }

  /* Cuadrícula de características inferior */
  .features-line {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 20px;
    padding: 30px 10%;
    background: #000;
    border-top: 1px solid #222;
  }

  .feature-item {
    border-left: 2px solid #ffffff;
    padding-left: 15px;
  }

  .feature-item h3 {
    font-size: 0.9rem;
    margin: 0;
    text-shadow: 0 0 5px rgba(255,255,255,0.5);
  }

  /* Adaptación para Móviles */
  @media (max-width: 768px) {
    .hero-ad-block {
      flex-direction: column;
      text-align: center;
      padding: 40px 20px;
    }
    .hero-text-content h1 {
      font-size: 2.5rem;
    }
    .hero-image-block img {
      max-width: 300px;
    }
    .feature-item {
      border-left: none;
      border-bottom: 1px solid #222;
      padding: 0 0 15px 0;
    }
  }
</style>

<div class="hero-ad-block">
  <div class="hero-text-content">