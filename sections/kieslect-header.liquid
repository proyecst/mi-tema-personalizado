<div id="trust-v3" style="background: {{ section.settings.bg_color }}; padding: 40px 0; border: 5px solid red;">
  <div style="max-width: 1100px; margin: 0 auto; display: flex; flex-wrap: wrap; gap: 20px; justify-content: center;">
    
    {% for block in section.blocks %}
      <div style="background: #f4f4f4; padding: 20px; border-radius: 10px; min-width: 250px; text-align: center; color: #000;">
        <span style="font-size: 40px;">{{ block.settings.icon }}</span>
        <h3 style="margin: 10px 0;">{{ block.settings.title }}</h3>
        <p>{{ block.settings.description }}</p>
      </div>
    {% endfor %}

  </div>
</div>

{% schema %}
{
  "name": "Barra Salvavidas",
  "settings": [
    { "type": "color", "id": "bg_color", "label": "Color de Fondo", "default": "#ffffff" }
  ],
  "blocks": [
    {
      "type": "item",
      "name": "Bloque",
      "settings": [
        { "type": "text", "id": "icon", "label": "Emoji", "default": "📦" },
        { "type": "text", "id": "title", "label": "Título", "default": "Título Prueba" },
        { "type": "text", "id": "description", "label": "Descripción", "default": "Descripción de prueba" }
      ]
    }
  ],
  "presets": [
    {
      "name": "Barra Salvavidas",
      "blocks": [
        { "type": "item" },
        { "type": "item" }
      ]
    }
  ]
}
{% endschema %}