<section class="trust-bar-section">
  <div class="page-width">
    <div class="trust-bar-container">
      
      {%- for block in section.blocks -%}
        <div class="trust-item">
          {%- if block.settings.icon != blank -%}
            <span class="trust-icon">{{ block.settings.icon }}</span>
          {%- endif -%}
          <div class="trust-text">
            <strong>{{ block.settings.title }}</strong>
            {%- if block.settings.subtitle != blank -%}
              <p>{{ block.settings.subtitle }}</p>
            {%- endif -%}
          </div>
        </div>
      {%- endfor -%}

    </div>
  </div>
</section>

{% style %}
  .trust-bar-section {
    background-color: {{ section.settings.bg_color }};
    padding: 20px 0;
    border-top: 1px solid #e8e8e8;
    border-bottom: 1px solid #e8e8e8;
  }
  .trust-bar-container {
    display: flex;
    justify-content: space-around;
    flex-wrap: wrap;
    gap: 20px;
  }
  .trust-item {
    display: flex;
    align-items: center;
    gap: 12px;
    flex: 1;
    min-width: 200px;
    justify-content: center;
  }
  .trust-icon {
    font-size: 24px; /* Tamaño de los emojis o iconos */
    display: flex;
    align-items: center;
  }
  .trust-text strong {
    display: block;
    font-size: 14px;
    color: {{ section.settings.text_color }};
    margin: 0;
  }
  .trust-text p {
    font-size: 12px;
    color: {{ section.settings.text_color }};
    opacity: 0.8;
    margin: 0;
  }

  @media (max-width: 768px) {
    .trust-item {
      min-width: 45%;
      justify-content: flex-start;
    }
  }
{% endstyle %}

{% schema %}
{
  "name": "Barra de Confianza",
  "settings": [
    {
      "type": "color",
      "id": "bg_color",
      "label": "Color de fondo",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Color del texto",
      "default": "#333333"
    }
  ],
  "blocks": [
    {
      "type": "trust_item",
      "name": "Elemento",
      "settings": [
        {
          "type": "text",
          "id": "icon",
          "label": "Icono (Emoji o SVG)",
          "default": "🚚"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Título",
          "default": "Título"
        },
        {
          "type": "text",
          "id": "subtitle",
          "label": "Subtítulo (Opcional)"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Barra de Confianza",
      "blocks": [
        { "type": "trust_item", "settings": { "icon": "🚚", "title": "Envío Gratis" } },
        { "type": "trust_item", "settings": { "icon": "🔒", "title": "Pago Seguro" } },
        { "type": "trust_item", "settings": { "icon": "⚡", "title": "Entrega Rápida", "subtitle": "2–5 días" } },
        { "type": "trust_item", "settings": { "icon": "💬", "title": "Soporte 24/7" } }
      ]
    }
  ]
}
{% endschema %}